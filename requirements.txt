# Core
jinja2>=3.1.6          # meta-prompt templating in the plugin layer
pyyaml>=6.0            # config.yaml loading
requests>=2.31.0       # LocalJudge — OpenAI-compatible local evaluator

# SDK-based generation backends
anthropic>=0.109.2     # AnthropicGenerator — claude-opus-4-8 and friends
mistralai>=1.0.0       # MistralGenerator — mistral-large-latest and friends
openai>=1.0.0          # OpenAIGenerator — OpenAI + any /v1/chat/completions endpoint

# Local Hugging Face generation (HuggingFaceGenerator) — optional, heavy.
# Imported lazily, so only needed if you instantiate HuggingFaceGenerator.
transformers>=4.40.0
torch>=2.2.0
